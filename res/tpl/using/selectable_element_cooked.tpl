<div class="line" data-hash="{tag:hash}" data-lifetime="{tag:lifetime}" data-quantity_inc="{tag:quantity_inc}" data-quantity_left="{tag:quantity_left}" data-not_used_perc="{tag:not_used_perc}">
    <table>
        <tr>
            <td class="reagent">
                <span class="name">{tag:menu:name}</span>
                <span class="quantity_left">Залишилось: {tag:quantity_left} {tag:units:short_name}</span>
            </td>
            <td class="date">
                <span class="inc_date"><label>Приготовано:</label>{tag:inc_date}</span>
                <span class="dead_date"><label>Зіпсується:</label>{tag:dead_date}</span>
            </td>
            <!-- td class="composition">{tag:composition:html}</td -->
        </tr>
    </table>
</div>